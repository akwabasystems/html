
/**
 * Returns the HTML element with the specified ID
 *
 * @param {String} id       the ID of the element to retrieve
 * @return {Element} the HTML element with the specified ID
 */
export default (id) => document.getElementById(id);
